name: poker_module
description: Módulo de Texas Hold'em Poker (Flutter + GetX) com arquitetura modular feature-first — engine, avaliação de mãos e mesa jogável.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
